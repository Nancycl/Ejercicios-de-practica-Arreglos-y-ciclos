cd (te regresa al inicio )
cd .. (te regresa una carpeta atrás)
mkdir (para crear carpetas)
touch (crea un archivo de cualquier tipo)
ls (para ver todos los archivos que exiten)
nano (ayuda a acceder al archivo y poder escribir dentro de el )
cp (copia los archivos)
mv (mover o cambiar el nombre del archivo)
rm (es para borrar) 
rm -r (remueve completa la carpeta)
rm -ri (entra a la carpeta y elimina archivo por archivo)
----------------------------------
git init (para iniciar el repositorio)
git checkout "nombre de la rama" (para cambiar de rama en git)
git status (para checar el estado del repositorio)
git diff (muestra los cambios que se hicieron al archivo)
git add . (para agregar los cambios)
